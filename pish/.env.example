# Copia este archivo a ".env" antes de levantar el proyecto:  cp .env.example .env
# Estos valores son solo para uso LOCAL (laboratorio). No uses estas credenciales en producción.
POSTGRES_USER=phish
POSTGRES_PASSWORD=phish_local_only
POSTGRES_DB=phishlab
